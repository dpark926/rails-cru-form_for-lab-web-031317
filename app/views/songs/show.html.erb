<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>This is the Show page for Songs</h1>
    <h2>Contains the song's name, artist_id, genre_id</h2>

    <h1><%=@song.name%></h1>

    <p>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) %></p>
    <p>Genre: <%= link_to @song.genre.name, genre_path(@song.genre) %></p>

  </body>
</html>
